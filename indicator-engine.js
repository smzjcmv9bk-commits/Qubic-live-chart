(()=>{
const $=id=>document.getElementById(id);
const periods=[9,21,50,200];
const series={};
let rows=[];
let loading=false;
function ema(data,p){if(!Array.isArray(data)||data.length<p)return[];const k=2/(p+1);let seed=data.slice(0,p).reduce((s,x)=>s+(+x.close||0),0)/p,out=[{time:+data[p-1].time,value:seed}];let v=seed;for(let i=p;i<data.length;i++){v=(+data[i].close)*k+v*(1-k);out.push({time:+data[i].time,value:v})}return out}
function regression(data){if(!Array.isArray(data)||data.length<20)return{mid:[],up:[],down:[]};const n=Math.min(120,data.length),a=data.slice(-n),xs=a.map((_,i)=>i),ys=a.map(x=>+x.close);const sx=xs.reduce((s,v)=>s+v,0),sy=ys.reduce((s,v)=>s+v,0),sxx=xs.reduce((s,v)=>s+v*v,0),sxy=xs.reduce((s,v,i)=>s+v*ys[i],0),den=n*sxx-sx*sx;if(!den)return{mid:[],up:[],down:[]};const m=(n*sxy-sx*sy)/den,b=(sy-m*sx)/n;const residual=ys.map((y,i)=>y-(b+m*i));const sd=Math.sqrt(residual.reduce((s,v)=>s+v*v,0)/Math.max(1,n-2));const mid=a.map((x,i)=>({time:+x.time,value:b+m*i}));return{mid,up:mid.map(x=>({time:x.time,value:x.value+2*sd})),down:mid.map(x=>({time:x.time,value:x.value-2*sd}))}}
function ensureSeries(){const c=window.__QubicChart;if(!c)return false;periods.forEach(p=>{if(!series[p]){const color=document.querySelector(`.emaColor[data-ema="${p}"]`)?.value||'#ffffff';series[p]=c.addLineSeries({color,lineWidth:p===200?2:1,lastValueVisible:false,priceLineVisible:false,visible:false,priceFormat:{type:'price',precision:10,minMove:.0000000001}})}});if(!series.regMid){series.regMid=c.addLineSeries({lineWidth:2,lastValueVisible:false,priceLineVisible:false,visible:false,priceFormat:{type:'price',precision:10,minMove:.0000000001}});series.regUp=c.addLineSeries({lineWidth:1,lineStyle:2,lastValueVisible:false,priceLineVisible:false,visible:false,priceFormat:{type:'price',precision:10,minMove:.0000000001}});series.regDown=c.addLineSeries({lineWidth:1,lineStyle:2,lastValueVisible:false,priceLineVisible:false,visible:false,priceFormat:{type:'price',precision:10,minMove:.0000000001}})}return true}
function paint(){if(!ensureSeries()||!rows.length)return;periods.forEach(p=>{const box=$(`ema${p}`),on=!!box?.checked;try{series[p].setData(ema(rows,p));series[p].applyOptions({visible:on})}catch{}});const regOn=!!$('cvdToggle')?.checked,r=regression(rows);try{series.regMid.setData(r.mid);series.regUp.setData(r.up);series.regDown.setData(r.down);[series.regMid,series.regUp,series.regDown].forEach(s=>s.applyOptions({visible:regOn}))}catch{}try{window.__QubicVolumeSeries?.applyOptions({visible:!!$('volToggle')?.checked})}catch{}const ant=$('antStatus');if(ant)ant.style.display=$('antToggle')?.checked?'':'none'}
async function refresh(){if(loading)return;loading=true;try{const tf=window.__QubicChartCore?.timeframe||$('qtf')?.value||$('tf')?.value||'15m';const r=await fetch(`/api/mobile-candles?interval=${encodeURIComponent(tf)}&limit=1000&indicator=1&t=${Date.now()}`,{cache:'no-store'});const j=await r.json();if(r.ok&&j?.ok&&Array.isArray(j.candles)){rows=j.candles.filter(x=>Number.isFinite(+x.time)&&+x.close>0);paint()}}catch(e){console.warn('indicator refresh',e)}finally{loading=false}}
function save(id,val){try{localStorage.setItem('qIndicator:'+id,String(val))}catch{}}
function restore(id,def){try{const v=localStorage.getItem('qIndicator:'+id);return v==null?def:v==='true'}catch{return def}}
function bind(){periods.forEach(p=>{const box=$(`ema${p}`);if(box){box.checked=restore(`ema${p}`,false);box.addEventListener('change',()=>{save(`ema${p}`,box.checked);paint()})}const col=document.querySelector(`.emaColor[data-ema="${p}"]`);if(col)col.addEventListener('input',()=>{try{series[p]?.applyOptions({color:col.value})}catch{}})});[['volToggle',true],['cvdToggle',true],['antToggle',true]].forEach(([id,def])=>{const e=$(id);if(e){e.checked=restore(id,def);e.addEventListener('change',()=>{save(id,e.checked);paint()})}});window.addEventListener('qubic:timeframe',()=>setTimeout(refresh,80));['tf','qtf'].forEach(id=>$(id)?.addEventListener('change',()=>setTimeout(refresh,100)));document.querySelectorAll('#quickTf [data-tf]').forEach(b=>b.addEventListener('click',()=>setTimeout(refresh,120)));setTimeout(refresh,350);setInterval(()=>{if(!document.hidden)refresh()},15000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();
window.__QubicIndicators={refresh,paint};
})();
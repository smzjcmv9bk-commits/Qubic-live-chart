(()=>{
'use strict';
const VERSION='chart-markers-v2';
const TF={'1m':60,'2m':120,'3m':180,'5m':300,'10m':600,'15m':900,'30m':1800,'45m':2700,'1h':3600,'2h':7200,'3h':10800,'4h':14400,'6h':21600,'8h':28800,'12h':43200,'1d':86400,'3d':259200,'1w':604800};
const londonPartsFmt=new Intl.DateTimeFormat('en-GB',{timeZone:'Europe/London',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',second:'2-digit',hourCycle:'h23'});
let lastNativeKey='';
function timeframe(){return String(window.__QubicChartCore?.timeframe||document.getElementById('qtf')?.value||document.getElementById('tf')?.value||'15m')}
function londonOffsetSec(ts){const d=new Date(+ts*1000),p=londonPartsFmt.formatToParts(d).reduce((o,x)=>(o[x.type]=x.value,o),{}),asUTC=Date.UTC(+p.year,+p.month-1,+p.day,+p.hour,+p.minute,+p.second)/1000;return asUTC-(+ts)}
function bucket(ts,tf){const sec=TF[tf]||900,off=londonOffsetSec(ts),local=ts+off;if(tf==='1w'){const d=new Date(local*1000),dow=(d.getUTCDay()+6)%7,mid=Date.UTC(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate())/1000,start=mid-dow*86400;let g=start-off;for(let i=0;i<2;i++)g=start-londonOffsetSec(g);return g}const localBucket=Math.floor(local/sec)*sec;let g=localBucket-off;for(let i=0;i<2;i++)g=localBucket-londonOffsetSec(g);return g}
function read(){let history=[],open=null;try{if(window.QUBIC_PAPER_BOT){history=window.QUBIC_PAPER_BOT.history?.()||[];open=window.QUBIC_PAPER_BOT.open?.()||null}else{history=JSON.parse(localStorage.getItem('qPaperTradesV1')||'[]');open=JSON.parse(localStorage.getItem('qPaperOpenV1')||'null')}}catch{}return{history:Array.isArray(history)?history:[],open}}
function nativeEntry(t,tf){const buy=t.side==='BUY';return{time:bucket((+t.openedAt||0)/1000,tf),position:buy?'belowBar':'aboveBar',color:buy?'#00d084':'#ff4d5e',shape:buy?'arrowUp':'arrowDown',text:`BOT ${buy?'BUY':'SELL'}`,size:2}}
function nativeExit(t,tf){if(!t.closedAt)return null;const good=(+t.resultPct||0)>=0;return{time:bucket((+t.closedAt||0)/1000,tf),position:t.side==='BUY'?'aboveBar':'belowBar',color:good?'#21c77a':'#ff6b78',shape:'circle',text:`EXIT ${good?'+':''}${(+t.resultPct||0).toFixed(2)}%`,size:1}}
function drawNative(){const s=window.__QubicCandleSeries;if(!s||typeof s.setMarkers!=='function')return false;const tf=timeframe(),{history,open}=read(),events=[];history.slice(-80).forEach(t=>{if(t?.openedAt)events.push({...nativeEntry(t,tf),_o:0,_id:String(t.id||t.openedAt)});const x=nativeExit(t,tf);if(x)events.push({...x,_o:1,_id:String(t.id||t.openedAt)})});if(open?.openedAt)events.push({...nativeEntry(open,tf),text:`OPEN ${open.side}`,_o:0,_id:String(open.id||open.openedAt)});events.sort((a,b)=>a.time-b.time||a._o-b._o||a._id.localeCompare(b._id));const clean=events.map(({_o,_id,...m})=>m),key=tf+'|'+JSON.stringify(clean);if(key===lastNativeKey)return true;try{s.setMarkers(clean);lastNativeKey=key;return true}catch(e){console.warn('paper native chart markers',e);return false}}
function overlayHost(){const host=document.getElementById('qchart');if(!host)return null;if(getComputedStyle(host).position==='static')host.style.position='relative';let o=document.getElementById('paperTradeChartOverlay');if(!o){o=document.createElement('div');o.id='paperTradeChartOverlay';o.style.cssText='position:absolute;inset:0;z-index:12;pointer-events:none;overflow:hidden';host.appendChild(o)}return o}
function eventRows(){const {history,open}=read(),rows=[];history.slice(-80).forEach(t=>{if(t?.openedAt)rows.push({kind:'ENTRY',side:t.side,ms:+t.openedAt,price:+t.entry,id:String(t.id||t.openedAt)});if(t?.closedAt)rows.push({kind:'EXIT',side:t.side,ms:+t.closedAt,price:+t.exit,pnl:+t.resultPct||0,id:String(t.id||t.openedAt)+'-x'})});if(open?.openedAt)rows.push({kind:'OPEN',side:open.side,ms:+open.openedAt,price:+open.entry,id:String(open.id||open.openedAt)+'-open'});return rows}
function drawOverlay(){const chart=window.__QubicChart,series=window.__QubicCandleSeries,host=overlayHost();if(!chart||!series||!host||typeof chart.timeScale!=='function'||typeof series.priceToCoordinate!=='function')return false;const tf=timeframe(),scale=chart.timeScale(),rows=eventRows();host.innerHTML='';let visible=0;for(const e of rows){if(!e.ms||!Number.isFinite(e.price)||e.price<=0)continue;const t=bucket(e.ms/1000,tf);let x=null,y=null;try{x=scale.timeToCoordinate(t);y=series.priceToCoordinate(e.price)}catch{}if(!Number.isFinite(x)||!Number.isFinite(y))continue;const buy=e.side==='BUY',exit=e.kind==='EXIT',good=e.pnl>=0;const el=document.createElement('div');const bg=exit?(good?'#177a50':'#a83744'):(buy?'#007f58':'#a92f40');const border=exit?(good?'#43e49a':'#ff8390'):(buy?'#43f0ac':'#ff7180');const arrow=exit?'●':buy?'▲':'▼';const txt=exit?`EXIT ${good?'+':''}${e.pnl.toFixed(2)}%`:`${e.kind==='OPEN'?'OPEN':'BOT'} ${e.side}`;el.textContent=`${arrow} ${txt}`;el.style.cssText=`position:absolute;left:${Math.round(x)}px;top:${Math.round(y)}px;transform:translate(-50%,${buy&&!exit?'8px':'-120%'});padding:3px 6px;border:1px solid ${border};border-radius:6px;background:${bg};color:#fff;font:900 9px/1.15 ui-monospace,SFMono-Regular,Menlo,monospace;white-space:nowrap;box-shadow:0 2px 8px rgba(0,0,0,.45)`;host.appendChild(el);visible++}window.__QubicPaperChartMarkers={version:VERSION,timeframe:tf,count:rows.length,visible,updatedAt:Date.now(),nativeSupported:typeof series.setMarkers==='function'};return true}
function refresh(){drawNative();drawOverlay()}
window.addEventListener('qubic:timeframe',()=>{lastNativeKey='';setTimeout(refresh,250)});
window.addEventListener('resize',()=>setTimeout(drawOverlay,120));
setInterval(refresh,1200);setTimeout(refresh,1200);
window.QUBIC_PAPER_MARKERS={version:VERSION,refresh:()=>{lastNativeKey='';refresh();return window.__QubicPaperChartMarkers||null}};
})();